export const test = (req, res) => {
    res.json({
        message: 'API is working!', 
    }); 
}

// req=> is what the client sends to the server/browser
// res => is what the server sends to the client/browser
